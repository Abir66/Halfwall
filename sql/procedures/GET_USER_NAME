CREATE OR REPLACE FUNCTION "GET_USER_NAME" (Q_USER_ID IN NUMBER)
RETURN VARCHAR2
AS
USER_NAME VARCHAR2(255);
BEGIN
	SELECT NAME INTO USER_NAME 
	FROM USERS
	WHERE USER_ID = Q_USER_ID; 
	RETURN USER_NAME;
END;