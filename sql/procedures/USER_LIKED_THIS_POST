CREATE OR REPLACE FUNCTION "USER_LIKED_THIS_POST" (Q_USER_ID IN NUMBER, Q_POST_ID IN NUMBER)
RETURN NUMBER
AS
	USER_LIKED NUMBER;
BEGIN
	USER_LIKED := 0;
	SELECT 1 INTO USER_LIKED FROM LIKES WHERE POST_ID = Q_POST_ID AND USER_ID = Q_USER_ID;
	RETURN USER_LIKED;
END;